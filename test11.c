#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main (void){
	int n1, n2, n3, med, min, max;
	printf ("\nIntroduzca 3 numeros");
	scanf ("%d", &n1);
	scanf ("%d", &n2);
	scanf ("%d", &n3);
	if (n2 > n1 && n2 < n3){
	n2 == med;
	else (n1 == min, n3=max);
	}
		if (n1 > n2 && n1 < n3){
		n1 == med;
		else(n2 == min, n3 == max);
		}
			if(n3 > n1 && n3 < n2){
			n3 == med;
			else(n1 == min, n2 == max);
			}
				if(med < min ){
				max == min;
				min == max;
				else printf("%\nmin, %\nmed, %\nmax")
				}
	}
return 0
}
